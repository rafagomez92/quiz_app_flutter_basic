import 'package:flutter/material.dart';

class StartaScreen extends StatelessWidget {
  const StartaScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Decoration"));
  }
}